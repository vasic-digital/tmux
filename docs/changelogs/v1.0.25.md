# vasic-digital tmux — v1.0.25 Release Notes

**Revision:** 1
**Last modified:** 2026-06-16T19:00:00Z
**Version:** 1.0.25 / versionCode 26
**Released:** 2026-06-16
**Hosts validated:** Mistborn (Darwin arm64) + nezha (ALT Linux x86_64)

---

## Summary

v1.0.25 is a **governance + tooling currency** release (no tmux product-surface
change) per the operator mandate "we MUST ALWAYS target the latest versions of all
our submodules": the HelixConstitution submodule is advanced to its latest upstream,
host-level codegraph version skew is fixed, and dead test-socket cruft is cleaned.

---

## Changed

- **HelixConstitution submodule `3f4d690` → `1d408cb`** (+67 commits, new anchors
  §11.4.140–158). Functional inheritance verified: `18_constitution_inheritance.sh`
  **PASS=10/0**. Release gate unaffected (`verify.sh` enforces §11.4.87..99, already
  mirrored).
- **Containers submodule** confirmed at latest `18ed03d`.

## Fixed

- **Tests 12 & 14 (destructive cgroup OOM) — swap-aware OOM (§11.4.81/§11.4.50).** Failed
  deterministically on nezha after the host gained 15 GiB swap (the cgroup enforced MemoryMax
  via swap, `oom_kill`=0). Fix: set `memory.swap.max=0` on the test scope so exceeding the cap
  OOM-kills deterministically (verified `oom_kill` 0 → 295). Now PASS 3/3 each.
- **codegraph macOS skew** (host-level) — stale `~/.local/bin/codegraph` symlink (→ v0.9.9)
  shadowed the npm-managed 1.0.1; repointed → both hosts resolve **1.0.1**.
- **macOS host hygiene** — removed **668 dead tmux test sockets** (678 → 10), no live session touched.

## Validation (dual-host release gate)

| Host | gate | meta |
|---|---|---|
| Mistborn (Darwin arm64) | run_all GREEN | — |
| nezha (ALT Linux x86_64) | verify.sh GREEN (destructive) | 52 CAUGHT / 0 ESCAPED |

`setup.sh` re-run + verified GREEN on both hosts; both reinstalled at v1.0.25.

## Known follow-up (non-release-blocking)

- Verbatim/cascade mirroring of §11.4.140–158 into `CLAUDE.md`/`AGENTS.md`/`QWEN.md` for
  non-`@import` agents (functional inheritance already works via the import; release gate
  does not enforce these).
- Operator-pending on nezha: `sudo bash scripts/build_oom_set.sh --install` + `sudo
  apt-get install -y stress-ng` → promotes tests 08/12/14 from honest SKIP to PASS.

## Files changed

- `Containers`/`constitution` submodule pointers; `VERSION` (1.0.25/26); `CHANGELOG.md`;
  `CONTINUATION.md`.

## Sources verified 2026-06-16

- <https://github.com/HelixDevelopment/HelixConstitution>
- <https://docs.npmjs.com/cli/v10/commands/npm-install>
