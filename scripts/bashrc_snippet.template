# ─── vasic-digital optimized tmux ─────────────────────────────────────────────
# Auto-generated by scripts/setup_tmux.sh on __DATE__
# Removes when scripts/setup_tmux.sh --uninstall is run.
# Source: __PROJECT__/tmux/build/bin/tmux + __PROJECT__/scripts/tmx
ATMOSPHERE_TMUX_DIR="__PROJECT__/scripts/tmux"
if [ -x "$ATMOSPHERE_TMUX_DIR/tmx" ]; then
    export PATH="$ATMOSPHERE_TMUX_DIR:$PATH"
    alias tmux='tmx'
fi
# ─── end vasic-digital optimized tmux ─────────────────────────────────────────
